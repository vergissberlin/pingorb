# Agent Instructions

## Workflow

- This repository uses **trunk-based development**. Commit directly to
  `main`.
- Do not create feature branches or pull requests for changes - commit and
  push straight to `main`.
